# .env file for organization-level GitHub Actions runner
#
# Set the scope to 'orgs' for an organization runner
RUNNER_SCOPE=orgs

# The full URL of the GitHub organization (e.g., https://github.com/yourorg)
REPO_URL=https://github.com/ORGANIZATION

# Personal Access Token with 'repo' and 'workflow' scopes
ACCESS_TOKEN=your_personal_access_token_here

# Name for this runner instance (optional, defaults to container hostname)
RUNNER_NAME=org-runner

# Comma-separated list of labels for workflow targeting (optional)
LABELS=docker,org

# Docker group ID from the host system (must match host's docker group for Docker-in-Docker)
# You can get this value with: getent group docker | cut -d: -f3
DOCKER_GID=999

